[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gradient-echoes"
version = "0.1.0"
description = "Educational optimization library (classical + quantum-ready examples)"
requires-python = ">=3.9"
